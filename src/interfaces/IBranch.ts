export default interface IBranch {
  id: string;
  name: string;
  address: string;
  googleMapUrl: string;
  phone: string;
  pictureUrl?: string;
  googleMapEmbedUrl?: string;
  description?: string;
}
